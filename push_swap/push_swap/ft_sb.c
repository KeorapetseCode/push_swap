
#include "push_swap.h"

B_STACK		*sort_first_two(B_STACK *b_ptr)
{
	int			temp_1;
	B_STACK		*struct_ptr;

	if (b_ptr == NULL)
		ft_putendl("Nothing In Stack B");
	if (b_ptr->next == NULL)
		ft_putendl("Only one structure found");
}